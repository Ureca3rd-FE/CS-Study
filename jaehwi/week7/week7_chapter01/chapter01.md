# 키(Key)

[Key | 👨🏻‍💻 Tech Interview](https://gyoogle.dev/blog/computer-science/data-base/Key.html)

**<목차>**

---

# 키(Key)란?

## 키(Key)의 개념

<aside>
💡

데이터베이스(DB)에서 조건에 만족하는 튜플을 찾거나 순서대로 정렬할 때, 다른 튜플들과 구별할 수 있는 유일한 기준이 되는 Attribute(속성)

</aside>

- 튜플 : 릴레이션을 구성하는 각각의 행(= Row) → 속성의 모임으로 구성됨
- 속성 : 릴레이션의 열(= Column) → 개체를 구성하는 속성들

## 키(Key)의 특성

> **유일성(uniqueness)**
> 

하나의 릴레이션에서 모든 튜플은 서로 다른 키 값을 가져야 함

> **최소성(minimality)**
> 

꼭 필요한 최소한의 속성들로만 키를 구성해야 함

*ex) 학생은 학번만 있어도 구별 가능*

# 키(Key)의 종류

## 1. **Candidate Key (후보키)**

<aside>
💡

튜플을 유일하게 식별하기 위해 사용하는 속성들의 부분 집합 (기본키로 사용 가능한 속성들)

</aside>

유일성과 최소성을 모두 만족…

- 유일성 : Key로 하나의 튜플(Tuple)을 유일하게 식별할 수 있음 (= 서로 다른 키 값을 가짐)
- 최소성 : 꼭 필요한 속성으로만 구성됨

**모든 릴레이션은 반드시 하나 이상의 후보키를 가져야 함!**

## 2. **Primary Key (기본키)**

<aside>
💡

후보키 중, 기본적으로 사용하기 위해 선택한 Main Key

(후보키 = 기본키에 대한 후보)

</aside>

기본키의 특징

- 한 릴레이션에서 **특정 튜플을 유일하게 구별할 수 있는 속성**
- **Null 값을 가질 수 없음** (개체 무결성의 첫번째 조건)
- 기본키로 정의된 속성에는 **동일한 값이 중복되어 저장될 수 없음** (개체 무결성의 두번째 조건)

## 3. **Alternate Key (대체키)**

<aside>
💡

후보키가 둘 이상일 때 기본키를 제외한 나머지 후보키들

</aside>

대체키의 특징

- 기본키로 **선택되지 못한** 후보키를 뜻함
- **‘보조키’**라고도 불림

## 4. **Super Key (슈퍼키)**

<aside>
💡

한 릴레이션 내에 있는 속성들의 집합으로 구성된 키

</aside>

- 유일성은 만족하지만, 최소성은 만족시키지 못함
    
    유일성을 만족해야 한다 → 속성 또는 속성들이 서로 다른 키 값을 가져야 함
    
      ex) 아이디, 비밀번호 등…
    

## 5. **Foreign Key (외래키)**

<aside>
💡

다른 릴레이션의 기본키를 참조하는 속성 또는 속성들의 집합

</aside>

**릴레이션들 간의 관계를 표현**

- 참조하는 릴레이션 : 외래키를 가진 릴레이션
- 참조되는 릴레이션 : 외래키가 참조하는 기본키를 가진 릴레이션

외래키의 특징

- 외래키 속성과 그것이 참조하는 기본키 속성의 이름은 달라도 되지만 도메인은 같아야 함
- 하나의 릴레이션에는 외래키가 여러 개 존재할 수도 있고, 외래키를 기본키로 사용할 수도 있음
- 같은 릴레이션의 기본키를 참조하는 외래키도 정의할 수 있음
- 외래키 속성은 NULL 값을 가질 수도 있음

---

*참고 자료*

[[데이터베이스] 키(Key)의 개념 및 종류](https://velog.io/@letskuku/%EB%8D%B0%EC%9D%B4%ED%84%B0%EB%B2%A0%EC%9D%B4%EC%8A%A4-%ED%82%A4Key%EC%9D%98-%EA%B0%9C%EB%85%90-%EB%B0%8F-%EC%A2%85%EB%A5%98#%EC%8A%88%ED%8D%BC%ED%82%A4super-key)

[[DataBase] 키(Key)의 개념 및 종류](https://limkydev.tistory.com/108#google_vignette)