# HTTP & HTTPS

## HTTP

HyperText Transfer Protocol

클라이언트와 서버 사이에서 데이터를 주고받는 규칙(프로토콜)
= `서버가 수행할 동작을 요청하는 방법`

### 특징

- 상태 정보를 저장하지 않음; Stateless
- 요청에 맞는 응답을 보낸 후 연결을 끊음; Connectionless

#### 장점

- 통신간의 연결 상태 처리나 상태 정보 관리를 할 필요가 없어 서버 디자인이 간단함
- 각 HTTP 요청에 독립적으로 응답만 보내주면 ok

#### 단점

- 이전 통신의 정보를 모르기 때문에 매번 인증 필요 > 이를 해결하기 위해 쿠키, 세션을 사용
- HTTP 자체는 보안이 안 됨 (평문 데이터를 전송하기 때문에 신호를 빼앗으면 다 읽을 수 있음)

### 메소드

#### 주요 메소드

- GET: 조회
- POST: 동봉하는 요청 데이터 처리(주로 등록)
- PUT: 리소스 전체 대체+없으면 생성
- PATCH: 리소스 부분 변경
- DELETE: 리소스 삭제

#### 기타 메소드

- HEAD: body를 제외하고 state와 header만 get
- OPTIONS: 대상 리소스에 대한 통신 옵션을 설명(주로 CORS에서 사용)
- CONNECT: 대상 자원으로 식별되는 서버에 대한 터널 설정
- TRACE: 리소스에 대한 경로를 따라 메시지 루프백 테스트 수행

## HTTPS와의 차이

HTTPS: HyperText Transfer Protocol Secure

위에서 언급되었듯 HTTP는 평문 데이터를 전송할 뿐이라 정보가 다 털린다.

여기에 SSL 프로토콜을 사용해 암호화를 추가하면 HTTPS.

- SSL: Secure Socket Layer, 인터넷으로 전달하는 정보를 보호하기 위해 개발한 통신 규약<br>
  공개키 암호화 방식으로 텍스트를 암호화함.

하여 원래라면 TCP와 직접 통신하던 `HTTP`가 `HTTPS`에선 SSL과 통신하고, SSL이 TCP와 통신하여 암호화와 증명서, 안전성 보호가 된다.

## 페이지에 접속할 때 과정

1. 사용자가 브라우저에 url 입력
2. DNS 서버에 도메인 네임으로 서버의 진짜 IP 주소를 찾음
3. IP 주소로 웹 서버에 TCP3 handshake를 거쳐 연결
4. 클라이언트가 웹 서버로 HTTP 요청 메시지 전달
5. 웹 서버는 HTTP 응답 메시지 전달
6. 도착한 HTTP 응답 메시지가 웹 페이지 데이터로 변환, 웹 브라우저에 출력

## 추가

🎈GET과 POST 방식의 차이

> - GET은 데이터가 URL에 노출되어 보안적으로 중요한 데이터가 유출될 가능성이 있다. 또한 담을 수 있는 데이터의 값이 한정적이다.
> - POST는 body에 데이터를 담기 때문에 GET보다는 안전하다(완전 안전X) 또한 GET보다 이미지를 포함한 대량의 데이터를 보낼 수 있다.

## 참고 자료

- [🌐 HTTP 메서드 종류 & 요청 흐름 💯 총정리](https://inpa.tistory.com/entry/WEB-%F0%9F%8C%90-HTTP-%EB%A9%94%EC%84%9C%EB%93%9C-%EC%A2%85%EB%A5%98-%ED%86%B5%EC%8B%A0-%EA%B3%BC%EC%A0%95-%F0%9F%92%AF-%EC%B4%9D%EC%A0%95%EB%A6%AC)
- [신입 개발자 기술면접 질문 정리 - 네트워크](https://dev-coco.tistory.com/161)

나중에 쿠키, 세션, 토큰도 정리하면 좋을듯
