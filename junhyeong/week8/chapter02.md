# 7주차 🛅데이터베이스

## Chap02. 정규화 (Normalization)

### #1. 정규화란?

#### 개념

- **데이터 중복을 최소화**하고, **데이터 무결성을 유지**하기 위해 테이블 구조를 보다 체계적으로 설계하는 과정
- 한 테이블에 하나의 개념만 담도록 함
- 즉 데이터가 논리적으로 일관성 있게 관리되도록 **테이블을 분리하는 과정**

#### 특징

- **데이터 중복 최소화**: 동일한 정보 반복 저장되는 문제 방지
- **데이터 무결성 유지**: 한 곳의 변경이 전체에도 일관되게 반영
- **이상 현상 방지**: 삽입/삭제/갱신 시 발생하는 데이터 불일치 문제 방지
- **데이터 구조 단순화**: 테이블 간 관계가 명확해짐 + 관리 쉬워짐
- **데이터 종속성 정리**: 부분 종속/이행적 종속을 제거하여 데이터가 주 식별자에게 종속되도록 설계

---

### #2. 정규화 단계

#### 제 1정규화 (1NF: First Normal Form)

- 개념
  - 모든 컬럼이 단일값(Atomic Value)만 가지도록 하는 단계
  - 중복 컬럼, 배열형 데이터를 제거
- 예시
  - 1NF 전
    |학번|이름|과목|
    |:--|:--|:--|
    |101|홍길동|DB, OS|
  - 1NF 후
    |학번|이름|과목|
    |:--|:--|:--|
    |101|홍길동|DB|
    |101|홍길동|OS|

#### 제 2정규화 (2NF: Second Normal Form)

- 개념
  - 1NF를 만족하면서 **부분 함수적 종속**을 제거하는 단계 -> **완전 함수적 종속**을 만족
  - 기본키의 일부분에만 종속된 컬럼이 존재하지 않도록 분리 -> 모든 비주요 속성이 기본키 전체에 종속되어야 함
  - 기본키가 단일키라면 제 2정규화 적용할 필요 없음 -> 부분 종속 개념 자체가 없으므로
- **부분 함수적 종속 (Partial Functional Dependency)**
  - 함수 종속: 속성 A가 속성 B의 값을 유일하게 결정한다면, B는 A에 함수적으로 종속되었다 하고 함
  - 테이블의 키가 복합키일 때, 기본키 전체가 아닌 일부 컬럼(부분 집합)만으로 다른 속성이 결정되는 상태
  - 기본키가 (A, B)이고 속성 C가 A에만 종속된 상태 -> C는 기본키 전체에 종속된 것이 아니므로 부분 종속 상태임
    ```
    (A, B) -> C
    A -> C
    ```
- 예시

  - 2NF 전
    |학번|과목|교수명|
    |:--|:--|:--|
    |101|DB|김교수|
    |101|OS|박교수|
  - 2NF 후: 수강 테이블과 교수 테이블
    |학번|과목|
    |:--|:--|
    |101|DB|
    |101|OS|

    | 과목 | 교수명 |
    | :--- | :----- |
    | DB   | 김교수 |
    | OS   | 박교수 |

#### 제 3정규화 (3NF: Third Normal Form)

- 개념
  - 2NF를 만족하면서 **이행적 종속**을 제거하는 단계
  - 비주요 속성이 다른 비주요 속성에 종속되는 관계 -> 비주요 속성은 오직 기본키에만 종속되어야 함
- **이행적 종속 (Transitive Dependency)** 의 상황

  - 상황 1
    - 3개의 컬럼 A, B, C가 있음
    - A -> B 이고, B -> C 라면, A -> C 도 성립함 -> 이행적 종속 상태
    - 기본키가 A라면, 테이블1 (A, B)와 테이블2 (B, C)로 분해
  - 상황 2
    - 4개의 컬럼 A, B, C, D가 있음. 기본키는 (A, B)임.
    - (A, B) -> C 이고, (A, B) -> D 이면서, C -> D 라면, 이행적 종속 상태
    - 테이블1 (A, B, C)와 테이블2 (C, D)로 분해

- 예시

- 3NF 전
  |학번|이름|학과코드|학과명|
  |:--|:--|:--|:--|
  |101|홍길동|CS|컴퓨터공학|
- 3NF 후: 학생 테이블과 학과 테이블
  |학번|이름|학과코드|
  |:--|:--|:--|
  |101|홍길동|CS|

  | 학과코드 | 학과명     |
  | :------- | :--------- |
  | CS       | 컴퓨터공학 |

#### 보이스 코드 정규화 (BCNF: Boyce-Codd Normal Form)

- 개념

  - 3NF보다 강화된 상태 -> 모든 결정자는 후보키여야함 (결정자 문제 해결)
  - 비후보키가 다른 속성을 결정하는 경우를 제거
  - 함수 종속 문제로 인한 이상 현상을 완전히 제거 가능

- 중간 요약

  - 상황 예시: 테이블의 속성(A, B, C, D, E). 기본키 (A, B).
  - 용어
    - 결정자 ~= 주요 속성: 기본키, 후보키에 포함되는 속성
    - 종속자 ~= 비주요 속성: 비키 속성
  - 2NF
    - 비주요 속성이 기본키 전체에만 종속
    - 기본키의 부분집합이 비주요 속성을 결정하는 상황 제거
    - 예시: B -> E인 상황 제거: 테이블1(A, B, C, D), 테이블2(B, E)
  - 3NF
    - 비주요 속성이 기본키에만 종속
    - 기본키가 아닌 속성이 비주요 속성을 결정하는 상황 제거
    - C -> D인 상황 제거: 테이블1(A, B, C), 테이블3(C, D) 테이블2(B, E)
  - BCNF
    - 모든 결정자는 후보키여야함
    - C -> A인 상황 제거: 테이블1(C, A), 테이블3(B, C, D), 테이블2(B, E)

- 예시

  - BCNF 전
    |교수명|과목|교재|
    |:--|:--|:--|
    |김교수|DB|DB입문|
    |박교수|OS|운영체제개론|
    |김교수|OS|운영체제개론|
  - BCNF 후: 교수-과목 테이블, 과목-교재 테이블
    |교수명|과목|
    |:--|:--|
    |김교수|DB|
    |박교수|OS|

    | 과목 | 교재         |
    | :--- | :----------- |
    | DB   | DB입문       |
    | OS   | 운영체제개론 |

#### 제 4정규화 (4NF)

- 개념
  - BCNF를 만족하면서 **다치 종속**을 제거하는 단계
  - 한 테이블에 2개 이상의 독립된 다중값 종속 존재하지 않도록 분리
- **다치 종속**: 하나의 속성이 2개 이상 이상의 서로 독립된 다중값 속성 집합을 결정
- 예시

  - 4NF 전
    |학번|취미|자격증|
    |:--|:--|:--|
    |101|독서|정보처리기사|
    |101|독서|컴활|
    |101|음악감상|정보처리기사|
    |101|음악감상|컴활|
  - 4NF 후: 학생-취미 테이블, 학생-자격증 테이블
    |학번|취미|
    |:--|:--|
    |101|독서|
    |101|음악감상|

    | 학번 | 자격증       |
    | :--- | :----------- |
    | 101  | 정보처리기사 |
    | 101  | 컴활         |

#### 제 5정규화 (5NF)

- 개념
  - 4NF를 만족하면서 **조인 종속**을 제거하는 단계
  - 조인 시 불필요한 중복/손실 발생하지 않도록 분리 -> 모든 조인 종속은 후보키에 의해 결정되어야함
- **조인 종속 (Join Dependency)**

  - 한 테이블이 여러개의 하위 테이블로 분해되었을 때,
  - 분해된 테이블들을 다시 조인했을 때, 원래 릴레이션이 복원되는 경우

- 예시

  - 5NF 전
    |학생|교수|과목|
    |:--|:--|:--|
    |홍길동|김교수|DB|
    |홍길동|이교수|OS|
    |박준형|김교수|DB|

  - 5NF 후: 학생-교수 테이블, 교수-과목 테이블, 학생-과목 테이블
    |학생|교수|
    |:--|:--|
    |홍길동|김교수|
    |홍길동|이교수|
    |박준형|김교수|

    | 교수   | 과목 |
    | :----- | :--- |
    | 김교수 | DB   |
    | 이교수 | OS   |

    | 학생   | 과목 |
    | :----- | :--- |
    | 홍길동 | DB   |
    | 홍길동 | OS   |
    | 박준형 | DB   |

---

### #3. 정규화의 장단점 & 비정규화(역정규화)

#### 정규화의 장점

- 데이터 중복 최소화 + 저장 공간 절약 (중복 데이터가 줄어드므로)
- 데이터 무결성 유지: 한 곳의 수정이 전체에 일관되게 반영
- 이상 현상 방지: 삽입/삭제/갱신 이상
- 데이터 구조 명확화: 테이블 관계 단순 + 구조 명확
- 유지보수 용이: 스키마 변경 시 영향 최소화

#### 정규화의 단점

- 조회 성능 저하: 테이블이 세분화되어 JOIN 연산 증가
- 쓰기 부하 증가: 여러 테이블에 걸쳐 데이터 변경 필요
- 복잡한 SQL 구조: 조회 시 여러 테이블을 조합해야하므로
- 읽기 중심 시스템에서 비효율적: 조회 중심 환경에서 속도 저하 발생
- 인덱스 관리 부담 증가: 테이블이 많아질수록 관리가 복잡해짐

#### 비정규화 (Denormalization)

- 개념
  - 정규화된 테이블 구조를 의도적으로 통합
  - 중복을 허용(테이블을 다시 합)하여 성능을 향상시키는 과정
  - 정규화는 데이터 무결성, 일관성을 중시하지만 조회 중심 시스템에서는 성능 저하를 유발할 수 있음
- 목적

  - 조회 성능 향상: JOIN 횟수를 줄임
  - 복잡한 쿼리 단순화: SQL 간결화로 애플리케이션 처리 효율 증가
  - 분석 쿼리 속도를 높이기 위함: 중복 데이터 일부 허용

- 적용 상황

  - 조회 빈도가 높을 때
  - JOIN 연산으로 인해 성능 저하 발생 시 -> 테이블 결합이 병목될 때
  - 데이터 변경이 적을 때 -> 읽기 중심 시스템 (통계, 리포트, ...)
  - 응답 속도가 중요한 경우 -> 사용자 인터페이스 지연 줄일 때

#### 비정규화 방법

- 테이블 병합: 자주 JOIN되는 테이블을 하나로 합침
- 중복 컬럼 추가: 특정 속성을 다른 테이블에도 복사하여 JOIN 없이 조회
- 집계 컬럼 추가: 이미 계산된 값을 컬럼에 저장
- 파생 데이터 저장: 계산 결과를 별도 컬럼으로 유지
- 부분 테이블 분리: 자주 사용하는 컬럼만 분리하여 조회 효율 향상

#### 비정규화 단점

- 데이터 중복 발생
- 불일치(불일관성) 가능성
- 무결성 유지 어려움: 중복 데이터 수정 시 다른 테이블에도 반영
- 갱신 비용 증가: UPDATE, DELETE 시 중복 데이터 모두 변경 필요

#### 정규화와 비정규화의 균형

- 실무에서 3NF, BCNF까지 적용하는 이유
  - 이상 현상, 종속성 문제를 거의 완전히 해결함
  - 데이터 불일치, 중복, 무결성 오류가 이미 사라짐
  - 4NF, 5NF는 이상 현상 방지 효과 대비 성능 손실이 더 커짐
- 4NF, 5NF를 거의 안쓰는 이유
  - 데이터 모델 복잡도 증가: 테이블 세분화가 오히려 관리/이해를 어렵게 함
  - JOIN 비용 급증 -> 성능 저하 발생
  - 이상현상 개선 효과 미미
  - 대부분 RDBMS에서 불필요: 트랜잭션/인덱스/제약조건으로 무결성 보장가능

---

### 퀴즈

#### 1번. 이행적 종속(Transitive Dependency)이 존재할 때 정규화 조치는? -> 4번

1. 해당 컬럼을 삭제한다
2. 모든 속성을 하나의 테이블에 합친다
3. 기본키를 변경한다
4. 중간 결정자(속성)를 별도의 테이블로 분리한다

#### 2번. BCNF를 위반하는 경우는? -> 3번

- 기본키는 (교수,과목) 이다.
- 즉, 각 번호 별로 가능/불가능 여부를 판단하고, 불가능한 이유가 BCNF 정규화를 해야하기 때문인 것을 고르는 문제

| 교수   | 과목 | 교재         |
| :----- | :--- | :----------- |
| 김교수 | DB   | DB입문       |
| 박교수 | OS   | 운영체제개론 |
| 김교수 | OS   | 운영체제개론 |
| 이교수 | DB   | DB입문       |

1. (교수, 과목) → 교재 => 가능: 문제에서 제시
2. 교수 → 과목 => 불가능: 하나의 교수 여러 과목 가능
3. 과목 → 교재 => BCNF 위반
4. (과목, 교재) → 교수 => 불가능: 후보키가 아님
