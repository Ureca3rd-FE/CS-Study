# 9주차 🛜네트워크

## Chap05. 대칭키 & 공개키

### #1. 대칭키 (Symmetric Key)

#### 개념

- 암호화와 복호화에 같은 키를 사용하는 방식
- 키가 동일: 속도가 빠르고 효율적
- 대표 알고리즘: AES, DES, 3DES 등

#### 특징

- 장점
  - 연산이 빠름
  - 대용량 데이터 암호화에 적합
- 단점
  - 키를 상대에게 전달하는 과정에서 키 교환 문제 발생
  - 키가 유출되면 보안이 깨짐

#### 사용 예시

- VPN, 파일 암호화, SSL 통신 중 세션 암호화 등

---

### #2. 비대칭키 (Asymmetric Key)

#### 개념

- 두 개의 키(공개키, 개인키)를 사용하는 암호 방식
- 공개키로 암호화 및 개인키로 복호화
- 개인키로 서명 -> 공개키로 검증
- 대표 알고리즘: RSA, ECC, Diffie-Hellman

#### 특징

- 장점
  - 키 교환 문제가 없음
  - 공개키를 누구나 받을 수 있음
- 단점
  - 대칭키에 비해 연산 속도가 느림
  - CPU 부담이 큼

#### 용도

- 인증, 전자서명, 키 교환, SSL 핸드셰이크 등

---

### #3. 공개키 암호화 방식 진행 과정

#### 기본 흐름

1. 송신자: 수신자의 공개키(Public Key)를 획득
2. 공개키로 메시지 암호화
3. 암호화된 데이터를 수신자에게 전송
4. 수신자: 본인의 개인키(Private Key)로 복호화

#### 특징

- 공개키는 누구나 알 수 있음
- 개인키는 수신자만 알고 있음
- 데이터 기밀성 (Confidentiality) 보장

---

### #4. SSL의 탄생 (대칭키와 공개키 암호화 방식의 혼합)

#### 문제상황 및 해결책

- 문제상황
  - 대칭키만 사용 시 빠르지만 키 교환이 위험
  - 공개키 암호화만 사용 시 보안은 좋지만 속도가 느림
- 해결책
  - 혼합 암호 (하이브리드 방식)를 사용
  - 대칭키와 비대칭키의 장점만 조합한 구조

#### 단계

1. 공개키 방식으로 세션키(Session Key)를 안전하게 교환
   - 클라이언트: 서버 공개키로 세션키를 암호화
   - 서버: 개인키로 복호화하여 세션키 획득
   - 키 교환이 안전하게 처리됨
2. 이후 실제 데이터 전송을 대칭키로 암호화
   - 속도 빠름
   - 안전함

#### 결과

- 비대칭키 방식으로 키 교환의 보안성 높임
- 대칭키 방식으로 통신의 속도를 향상
